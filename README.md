Required download dependency for this application

css:
npm install bulma bulma-extensions

sass:
npm install -g sass
npm install node-sass-chokidar --save-dev

npm run:
npm install npm-run-all --save-dev
